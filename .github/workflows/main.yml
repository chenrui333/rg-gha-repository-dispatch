name: Main

on:
  push:
    branches-ignore: "release*"
  repository_dispatch:
    types: ["blt-jenkins"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
        make echo-env
      - name: Ref
        if: github.ref == 'refs/heads/master'
        run: echo $GITHUB_REF
